from base_test import ArkoudaTest
import arkouda as ak
import aksparse as aks

class aksparseTest(ArkoudaTest):

    #TODO: write some small matrix test cases
    #(also likely fix a bunch of broken code)
    def test_aksparse(self):
        self.assertEqual(1,1)
